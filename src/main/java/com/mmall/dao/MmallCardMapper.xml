<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mmall.dao.MmallCartMapper">

    <select id="selectCartById" parameterType="int" resultType="MmallCart">
        select * from mmall_cart where id=#{id}
    </select>

</mapper>
